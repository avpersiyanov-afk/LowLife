# -*- coding: utf-8 -*-
__title__ = u"Параметры цепей\n(общее)"
__doc__ = (
    u"Настройки кнопки «Цепь (общее)»: тип электрической цепи, кабель "
    u"(строка справочника) и параметр цепи, куда он пишется, параметр цепи "
    u"«Имя нагрузки» и список параметров подключаемого устройства, из "
    u"которых оно собирается (через разделитель; один параметр = одиночный "
    u"источник), а также режим — отдельная цепь на каждое устройство или "
    u"все устройства в одну общую цепь.\n\n"
    u"Значения хранятся в отдельном файле и подставляются при каждом "
    u"запуске кнопки «Цепь (общее)»."
)
__author__ = "Pipers"

from pyrevit import revit, forms

from lowlife.generic_circuits_settings import get_settings_interactive

doc = revit.doc

settings = get_settings_interactive(doc)

if settings is None:
    forms.alert(u"Операция отменена.", exitscript=True)

forms.alert(u"Настройки сохранены.")
